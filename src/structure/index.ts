import * as dropdown_selectors from './DropdownNavMenu';
import * as switchspace_selectors from './SwitchSpace';

console.log(...Object.keys(switchspace_selectors),5)
export default {
    dropdown_selectors,
    switchspace_selectors
}